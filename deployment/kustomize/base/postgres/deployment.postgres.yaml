apiVersion: apps/v1
kind: Deployment
metadata:
  name: zah-sampleapp-deployment-postgres
  labels:
    app: zah-sampleapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: zah-sampleapp-postgres
  template:
    metadata:
      labels:
        app: zzah-sampleapp-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15.1
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: zah-sampleapp-postgres-secret
          env:
            - name: POSTGRES_HOST_AUTH_METHOD
              value: trust
          resources:
            requests:
              memory: "256Mi"
              cpu: "500m"
            limits:
              memory: "500Mi"
              cpu: "1"
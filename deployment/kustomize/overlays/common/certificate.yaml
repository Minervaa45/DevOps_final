apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: zah-final-task-certificate
  namespace: zah-final-task
spec:
  dnsNames:
  - 'zah.dacdevops.ru'
  issuerRef:
    kind: ClusterIssuer
    name: yc-clusterissuer
  secretName: zah-final-task-cert
